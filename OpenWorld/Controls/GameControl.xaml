<UserControl x:Class="OpenWorld.Controls.GameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls1="clr-namespace:OpenWorld.Controls"
             Background="#01808080">
    <Grid x:Name="_bk">
        <Canvas x:Name="_canvas">
            <Canvas.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="_scaleTransform"/>
                    <TranslateTransform x:Name="_translateTransform"/>
                </TransformGroup>
            </Canvas.RenderTransform>
            <Canvas x:Name="_mapCanvas"/>
            <controls1:HeroControl x:Name="_heroControl"/>
            <controls1:TargetControl x:Name="_targetControl"/>
        </Canvas>
    </Grid>
</UserControl>
